# Scratch / drop folder

Drop big original images, scans, PDFs etc. here. Everything in this folder is
git-ignored, so it is never committed or deployed. Resize/optimise into
`assets/images/` for use on the site (e.g. `sips -Z 800 assets/_tmp/big.png --out assets/images/small.png`).
